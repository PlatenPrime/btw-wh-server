/**
 * Холостой режим air-среза: при `true` все запросы к air возвращают -1/-1
 * без сетевых вызовов. Компенсация выполняется на клиенте.
 *
 * Для возврата к нормальной работе — установить в `false`.
 */
export const AIR_IDLE_MODE = true;
